# Token / Usage Ledger

Usage notes appended after each session, sourced from `/status` output the
user pastes. Tracked per `CLAUDE.md`'s Project Manager bookkeeping rule.

| Date | Sprint | Session window | Weekly window | Promo | Equiv. API value | Model | Lines added | Tests | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 2026-07-23 | Sprint 1 | 29% of 5h | 10% of weekly | +50% promo active through Aug 19 | $12.45 | claude-sonnet-5 | 1156 | 21 green | Includes ML-entity UAT fix detour (MLModel/MLFeatureTable registration) |
| 2026-07-26 | Sprint 2 close | cumulative (Sprint 2) | 3% of weekly (fresh week from Jul 24) | +50% promo active through Aug 19 | $65.93 | claude-sonnet-5 + claude-haiku-4.5 | +7,815 / −158 | 144 green, 2 live (excluded by default), 1 pre-existing fail (unrelated DataHub state) | Sentinel/Investigator/Orchestrator + `guardian` CLI, 5 slices + hands-on UAT punch list (2 real bugs found, both fixed). **92% of usage came from subagent-heavy sessions — Sprint 3 process change**: reserve subagents for distinct roles (design/test), do routine implementation in the main session. |
| 2026-07-26 | Sprint 3 WP1 (Scribe) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +1,637 / −11 | 182 green, 3 live (excluded by default), 0 fail | First session run entirely in the main session, no subagent delegation — the process change logged above, applied for the first time. Parts A-E: fixed a real drifted-state bug (Part A), designed and live-verified DataHub writeback (Part B), implemented `scribe.py` (Part C, 2 real bugs found via its own live test), the repo owner's own hands-on UI walkthrough found a 3rd real issue (Part D, ambiguous assertion descriptions), closed out per the new "commit at every Part/slice boundary, push at least daily" rule (added to `CLAUDE.md` this session, prompted by WP1 itself sitting uncommitted across several turns). |
| 2026-07-26 | Sprint 3 WP2 (Remediator) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +2,900 / −105 (approx.) | 266 green, 4 live (excluded by default), 0 fail | Second repo scaffolded (`denial-guardian-data-platform`, Part A), quarantine-not-correction design approved (Part B), `remediator.py` + `sql_validation.py` implemented (Part C). Part D: 2 real PRs opened against both real saved incidents — 3 real bugs found live in the first pass (generation timeout too short, DROP-breaks-dependent-views), then the repo owner's own hands-on UAT ("would I merge it?") caught a 4th, more serious one neither automated pass had: a fix that no longer bootstraps on a genuinely fresh database. Added a second, required validation pass (`fresh_build_validation.py`) plus `force=True` PR-update-in-place support, regenerated both PRs correctly on the first attempt, re-verified clean. Documented as decision 0008 (includes the amendment) and `sprint-3-wp2.md`'s own "the lesson" section. |
| 2026-07-27 | Sprint 3 WP3 (Reporter) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +900 / −60 (approx.) | 334 green, 4 live (excluded by default), 0 fail | `reporter.py` + templates implemented (Part B), plus an unplanned but necessary detour: `guardian resume` (`load_incident()`/`resume_incident()`), since re-running the full pipeline can never backfill an existing incident's fields (a fresh `incident_id` every time -> a new PR, not an update). Part C: two full rounds of real, hands-on UAT, each catching a genuine bug the automated suite couldn't — round 1 found raw agent telemetry (function names, file paths, SQL, tool-call syntax) leaking into the compliance-facing narrative, fixed by a Technical Appendix + plain-language translation; round 2, *during the re-check itself*, found the Actions Taken section collapsing a real, meaningful idempotency signal ("already present" vs "freshly applied") into one uniform word. Every finding left behind a permanent regression test, not just a fix — the repo owner's explicit standing instruction going forward. Documented as decision 0009 and `sprint-3-wp3.md`'s own "the lesson, twice over" section. |
| 2026-07-28 | Sprint 3 WP4 (Drift/feature-health check) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +1,050 / −25 (approx.) | 374 green, 6 live (excluded by default), 0 fail | LLD written first (design-only, explicit stop for review), including an empirically-verified honesty verdict: no genuine temporal baseline exists for this project's fixed-seed, single-generation dataset, so "drift" was redefined honestly as single-snapshot internal-consistency checks — a naive mean/std check on `billing_zscore` was proven tautological (not just assumed) both algebraically and against live data. The verification detour surfaced a real, separate bug: `src/datahub/README.md` instructed regenerating `healthcare.db` from scratch and falsely claimed `random.seed(42)` made it reproducible (the actual mechanism, SQLite's own `ORDER BY RANDOM()`, is unseeded by Python) — fixed and documented as decision 0010 before continuing. Implementation (`src/agents/drift.py`, `guardian check-drift`, Reporter's Model Health Check section) then hit a real, live-only bug the mocked test suite couldn't have caught: DataHub rejected the assertion writeback with a 422 because an MLModel can't be a native assertion target, only a dataset — fixed by retargeting `denial_model_scores` (documented as decision 0011). Proved live end-to-end against both real canonical incidents, including a direct idempotency proof (writeback called twice with the same finding, zero duplicate writes) and a stale test-fixture assumption caught and fixed by the same live run. This closes Sprint 3 — all four work packages (Scribe/Remediator/Reporter/Drift) now live-proven, not just unit-tested. |
| 2026-07-28 | **Sprint 3 close** | 40% of 5h | 28% of weekly | *user to fill from /status* | $270.45 (cumulative for Sprint 3) | claude-sonnet-5 | n/a (see individual WP rows above) | 374 green, 6 live (excluded by default), 0 fail | All 4 work packages (Scribe US-4, Remediator US-3, Reporter US-5, Drift US-6) spanned Jul 26-28 (three days, not same-day), still ~1 day ahead of the planned Jul 29-Aug 3 window for this sprint. Each work package was proven against the real, live DataHub instance and the real committed `healthcare.db`, not just a mocked test suite — every stage's writeback (Scribe's tag/doc/assertion, Remediator's PR idempotency, Reporter's report regeneration, Drift's assertion retargeting) was live-verified for idempotency specifically, not inferred from a clean exit. Claude Code itself flagged that 92% of this session's usage occurred above 150k tokens of context (long sessions cost more even with prompt caching); action taken: `/clear` before starting WP5/Sprint 4. |
| 2026-07-28 | Sprint 3 stretch, WP5 (FHIR Compliance Bridge) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +2,463 / −0 (approx.; incl. 726 lines of generated example JSON) | 397 green, 6 live (excluded by default), 0 fail | Half-day hard-capped thin slice, done in-chat (Part A design sketch, no standalone LLD) per explicit scope. The repo owner's specific ask at the design STOP: scrutinize the real-vs-placeholder line for the generated FHIR resources, since a demo compliance artifact that quietly overclaims regulatory readiness would undercut everything this project's honesty discipline has built so far. Result: `type` (a required FHIR field with no real source data) uses FHIR's own `data-absent-reason` extension instead of a fabricated claim-type code; diagnosis/adjudication-reason fields are `.text`-only with no invented ICD-10/CARC coding; `patient`/`insurer`/`provider` are display-only references, never a fake resolvable one. `fhir_export.py` + `guardian export-fhir` reuse Scribe's exact tag/doc-note writeback pattern (decision 0007) — no new DataHub mechanism. Proved live against both canonical incidents (both root-cause framings — `inherited_from:raw_patients` and `introduced_at:claims` — confirmed in the generated JSON), with idempotency proven at both the file-generation layer (byte-identical re-run) and the DataHub layer (tag/doc-note already-present on rerun). Documented as decision 0012 and `sprint-3-wp5.md`. Honest note on the cap: engineering time held, but live DataHub calls ran several minutes each almost entirely from the MCP subprocess's own telemetry-retry backoff (external latency, not this module's code) — recorded rather than left to make the wall-clock accounting look better than it was. |
| 2026-07-28 | Sprint 4 WP1 (fresh-clone judge simulation) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +570 / −24 (approx., across 4 Parts) | 408 green, 6 live (excluded by default), 0 fail | Four-part structure with an explicit repo-owner stop between B and C: Part A simulated a first-time judge in a genuinely fresh `git clone` from `origin` (not a local copy) — no root `README.md` existed anywhere (only a DataHub-dataset-scoped one), zero documentation for standing up the required local DataHub instance, and Investigator's `claude_code` backend silently lost all DataHub MCP tool access with no `.env` configured (confirmed via its own generated evidence: "8 separate ToolSearch attempts" all failing, falling back to raw SQL introspection to still reach the right answer). Part B: severity-ranked punch list (3 BLOCKING, 4 CONFUSING, 0 COSMETIC), reported and held for triage per the repo owner's explicit instruction — approved unchanged ("go ahead and fix all of them"). Part C root-caused rather than patched: the MCP-tools bug traced to `subprocess.run()` never passing an explicit `env=`, so `${DATAHUB_GMS_URL}` expanded to an empty string (present-but-empty defeats the SDK's own missing-key fallback) rather than any real default — fixed with a shared `_datahub_mcp_env()` resolver and a new `env=` param threaded through the backend interface, 7 regression tests. Separately discovered (reading `mcp-server-datahub`'s own source while root-causing the above) that `acryl-datahub`'s telemetry module was the actual cause of nearly all this project's live-command slowness across every prior sprint — `DATAHUB_TELEMETRY_ENABLED=false`, DataHub's own documented opt-out, cut a zero-config `guardian run` from 879s to 158.5s, `check-drift` from 8+ minutes to 3.4s, `export-fhir` from minutes to 1.8s, with telemetry retry warnings going from dozens per command to zero. Part D re-verified the entire loop from a second fresh clone of the pushed fixes (not asserted "looks fixed") and caught a live bug in one of Part C's own fixes in the process: the new progress-line `print()` had no `flush=True`, so it sat invisible in Python's stdout buffer for the whole run whenever output wasn't a real terminal — fixed immediately, re-verified a second time (line now appears within ~4s). Same "verify against the live system" discipline this project has held to every sprint, this time catching a bug in a fix for exactly that discipline. Documented as `sprint-4-wp1.md`; `src/datahub/README.md`'s "anonymous requests get a 401" claim corrected after being empirically disproven (a bare `curl` read and a real SDK write with `token=None` both succeeded against the live instance). |
| 2026-07-29 | Sprint 4 WP2 (README polish + open-source contribution) | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | *user to fill from /status* | claude-sonnet-5 | +150 / −5 (approx., docs only) | 408 green, 6 live (excluded by default), 0 fail (no code touched) | Two independently-approved halves. Part A polished the README (not a rewrite — WP1's setup sequence left untouched): a "The pitch" section mapping all four real hackathon tracks to the specific feature earning each one (corrected from an initial draft that cited only the one track name actually documented in this repo, "Production ML judging track" — right instinct not to invent the other three, then corrected once the repo owner supplied the real list from the original brief), a "Quick links" block, and an honest current-state line with measured numbers. Caught mid-review: asked to paste the actual section text for approval, the first paste arrived with words fused mid-token — the repo owner refused to accept "probably a rendering artifact" and demanded a falsifiable check (`sed -n` plus a `grep -c` for the exact garbled tokens) run for real; both confirmed the file itself was always clean, the corruption was introduced only when quoting into chat. Part B turned two real DataHub platform findings this project already made (decisions 0007 §3: no supported MLModel-to-Dataset lineage mechanism, three approaches independently rejected; 0011 §5: CustomAssertionInfo's error messages don't name their actual constraint) into two draft GitHub issues, recommended and approved as lead-plus-companion (the lineage finding as the stronger, evidence-heavy primary issue; the assertion finding reframed around error-message clarity rather than a contestable design opinion — same "report what was observed, don't prescribe the design" principle as Remediator's own PR framing, decision 0008). Before filing, the repo owner explicitly required the reproduction permalink's commit SHA be verified by loading the real rendered page, not re-derived locally — done via `WebFetch` against both permalinks (Chrome extension unavailable in this environment, substituted and stated plainly) confirming exact file/commit/line-range/quoted-text match. Filed via `gh issue create`: datahub-project/datahub#18742 (lineage) and #18743 (assertion messages), both verified post-filing via `gh issue view --json` (body byte-matches the approved draft on both; the `--label bug` flag silently no-opped on both, standard GitHub behavior for an external contributor without repo write access — reported, not hidden). Recorded as new sections on the originating decision docs (0007, 0011) rather than one combined new doc, and `sprint-4-wp2.md`. |
