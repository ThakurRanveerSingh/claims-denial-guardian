# Python virtual environment
.venv/

# Environment variables / secrets
.env

# Python bytecode cache
__pycache__/
*.pyc

# Pytest cache
.pytest_cache/

# OS cruft
.DS_Store

# Packaging build artifacts (pip install -e . / pyproject.toml, Slice 4,
# lld-sprint2.md §4.3) — generated metadata, not source; regenerated by
# `pip install -e .` on any machine, shouldn't be tracked.
*.egg-info/
build/
dist/
